🎨 Etch-a-Sketch (DOM Manipulation Project)

This project is a browser-based drawing pad inspired by the classic Etch-a-Sketch toy. It allows users to draw by hovering over a grid of squares, with options to resize the grid, clear the board, and toggle random colors.

This project was created as part of The Odin Project Foundations Course, specifically within a lesson focused on DOM manipulation.

🚀 Features
🟦 Dynamic Grid Creation
Generates a square grid (default: 16×16)
Adjustable size (1–100) via user input
🎯 Hover Drawing
Squares change color when hovered over
🎨 Color Modes
Default: black drawing
Toggle: random RGB color mode
🧹 Clear Function
Resets all squares without changing grid size
🔁 Resizable Canvas
Prompt-based resizing with input validation
🛠️ Technologies Used
HTML5
CSS3 (Grid layout)
JavaScript (ES6)
📂 Project Structure
.
├── index.html   # Structure of the page
├── index.css    # Styling and grid layout
└── script.js    # DOM manipulation and logic
📸 How It Works
The page loads a default 16×16 grid.
Each square is dynamically created using JavaScript.
Hovering over a square changes its color.
Buttons allow you to:
Resize the grid
Clear the board
Toggle random color mode
▶️ Getting Started

To run this project locally:

Clone or download the repository
Open index.html in your browser

No installation or dependencies required.

📚 What I Learned
Creating and appending DOM elements dynamically
Using event listeners (mouseenter, click)
Manipulating styles with JavaScript
Working with CSS Grid for layout
Handling user input with prompt()
Structuring small interactive web applications
🔮 Future Improvements
Add gradual shading/darkening effect
Add click-and-drag drawing (not just hover)
Mobile/touch support
UI improvements (color picker, slider instead of prompt)
Save/export drawings
🙌 Acknowledgements

This project was built as part of The Odin Project, an open-source curriculum for learning full-stack web development.

If you want, I can also tailor this to match your GitHub style (badges, live demo link, etc.) or generate a version specifically optimized for recruiters 👀